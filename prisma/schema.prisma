datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:Soban-0343@fb-logistics.c3iweekwc9sw.eu-central-1.rds.amazonaws.com"
}

generator client {
  provider = "prisma-client-js"
}

model foodtruck {
  id            Int      @id @default(autoincrement())
  name          String
  category      String
  description   String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  
  @@map("foodtrucks")
}
